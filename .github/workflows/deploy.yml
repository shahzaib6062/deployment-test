name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 14

      - name: Install dependencies
        run: npm install

      - name: Build and deploy to GitHub Pages (Staging)
        if: github.ref == 'refs/heads/staging'
        run: |
          npm run build
          npx gh-pages -d build -b gh-pages-staging -u "${{ secrets.GITHUB_TOKEN }}"

      - name: Build and deploy to GitHub Pages (Production)
        if: github.ref == 'refs/heads/main'
        run: |
          npm run build
          npx gh-pages -d build -b gh-pages-production -u "${{ secrets.GITHUB_TOKEN }}"
